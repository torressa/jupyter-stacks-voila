version: "2.3"

services:
  run-book:
    image: jupyter/book:run
    build:
      context: ../
      dockerfile: docker/Dockerfile
      target: run
      cache_from:
        - jupyter/book:builder
    volumes:
      - ../:/home/jovyan/work
    ports:
      - "8888:8888"
